export default function Page() {
  return (
    <section>
      <h1>Data-mapping</h1>
      <p>Associer libellé externe ↔ produit interne via <code>/api/v1/modules/data_mappings</code>.</p>
      <p>UI interactive : <code>resources/renderer/index.html</code></p>
    </section>
  );
}
