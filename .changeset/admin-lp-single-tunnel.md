---
"@creezio/factory": patch
---

Un repo admin pose tout seul `CREEZIO_DOMAIN=admin.{zone}` + `CREEZIO_TUNNEL_EXTRA_HOSTNAMES=lp.{zone}`. `migrate-stack` synchronise un stack déjà in-process (`sync-cf`). NPM n'est plus une voie d'exposition.
