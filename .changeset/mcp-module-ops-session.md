---
"@creezio/mcp-facade": patch
"@creezio/assistant": patch
---

Propager cookie / Bearer de la session chat OS dans la requête synthétique des tools `module.*` — plus de 401 `requireSession` in-process.
