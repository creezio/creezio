---
"@creezio/factory": patch
---

`creezio server-docker registry-gc` : GC fail-closed du registre Docker local (`registry:2`, `127.0.0.1:5000`) — API v2 list/delete, `--keep N` (défaut 2), tags des conteneurs en cours toujours conservés, `registry garbage-collect`, `--dry-run`.
