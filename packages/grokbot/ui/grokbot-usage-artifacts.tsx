"use client";

/**
 * Blocs Usage + Artefacts de l'agent ouvert — possédé par GROKBOT-1.
 * GROKBOT-2 ne touche pas ce fichier.
 *
 * Placeholder d'extraction : le câblage GET usage / artifacts arrive
 * dans le commit d'enrichissement GROKBOT-1.
 */

export function GrokbotUsageArtifacts(_props: {
  agentId: string | null;
  prUrl?: string | null;
}) {
  return null;
}
