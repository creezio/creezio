# `@creezio/mails`

Mails **natifs plateforme** — boîte / envoi générique. **Pas** de templates
TempoFlow/Fidu. Extension providers via `registerMailProvider`.

```ts
import { createMemoryMailsStore, createMailsApiMount } from "@creezio/mails";
```
